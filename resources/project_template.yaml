citations:
  - name: "muvis-align"
    license: "GPL 3"
    description: "Image registration"

docker_image:
  org: ccp-volume-em
  name: muvis-align
  tag: "1.0.0"
  platform: "linux/amd64"

inputs:
  - name: input_images
    type: image
    label: "Input images"
    description: "Input images"
    file_count: "multiple"
    unique_string:
      - "*"
#    default: "single"
    cli_tag: "--input_images"
    cli_order: 0
    optional: False
    format:
      - tiff
      - ometiff
      - zarr
      - omezarr
    section_id: "input_data"
    folder_name: "/data"
    mode: "beginner"

outputs:
  - name: output_array 
    type: image
    label: "Output Data"
    description: "output data"
    cli_tag: "--output_data"
    cli_order: 1
#    default: "single"
    optional: True
    format:
      - omezarr
    folder_name: "/data"
    file_count: "single"
    unique_string:
      - "*"
    section_id: "outputs"
    mode: "beginner"
    depth: True
    timepoints: True
    tiled: True
    pyramidal: True

parameters:
  - name: show_shapes
    type: checkbox
    label: "Show shape data"
    description: "If enabled, tiles will be displayed as shapes"
    default: True
    cli_tag: "--show_shapes"
    cli_order: 1
    optional: True
    append_value: False
    section_id: "layout_preview"
    mode: "beginner"
  - name: show_preview
    type: checkbox
    label: "Preview image data"
    description: "If enabled, tiles will be previewed as images"
    default: True
    cli_tag: "--show_preview"
    cli_order: 1
    optional: True
    append_value: False
    section_id: "layout_preview"
    mode: "beginner"

display_only:
  - name: metadata
    type: table
    label: "Metadata"
    description: "Metadata"
    section_id: "input_data"
